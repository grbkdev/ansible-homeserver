http:
  routers:
    hass:
      rule: Host(`hass.{{ traefik_domain }}`)
      service: hass
      tls:
        certResolver: letsencrypt
  services:
    hass:
      loadBalancer:
        servers:
          - url: "{{ traefik_hass_service_url }}"